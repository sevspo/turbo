{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 5000",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "api": "*"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.8",
    "config": "*",
    "tsconfig": "*"
  }
}
